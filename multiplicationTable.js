const multiplicationTable = function(maxValue) {
  // Your code here
};

console.log(multiplicationTable(1));
console.log(multiplicationTable(5));
console.log(multiplicationTable(10));

// make an array with the range from 1 to max number 

//loop that starting a new array of each number produced, with the step in the count being that number to the max of [i]X[max]

// put \n to add the lines after each loop